import 'package:flutter/material.dart';

class TaskMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Color.fromRGBO(255, 193, 68, 0),
      body: Padding(
        padding: EdgeInsets.all((10)),
      ),
    );
  }
}
